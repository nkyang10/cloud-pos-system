# QA gate — run `20260907-1646` (Stock Quantity Adjustments)

Canonical test command (stdlib `unittest`, no third-party deps):

    python3 -m unittest discover -s tests -v

(`python` is not on PATH in this environment; `python3` is the verified
invocation. Either name works on a normal install.)

## Status: PASS

- Full suite re-run at the end of the cycle after the final feature merge
  (`pos/store.py` re-applied via merge "theirs") and a QA cleanup that removed
  duplicate `_PRAGMAS` / `ItemNotFoundError` definitions left behind by that
  merge resolution (behavior unchanged):
  - Ran 34 tests in 0.101s — OK
- Coverage (see `TESTING.md`): store (`test_store.py`), HTTP add/list
  (`test_server.py`), `run.py` port resolution (`test_run.py`), movement-ledger
  store layer (`test_movements.py`, PRD AC2–AC5/AC7), and end-to-end
  `POST /items/{id}/adjust` (`test_adjust_http.py`, PRD AC6–AC7).
- No tests deleted or weakened to make the suite pass; the only source change
  was the duplicate-definition cleanup above.